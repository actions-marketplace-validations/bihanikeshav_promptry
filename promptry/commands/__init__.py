"""Sub-command groups for the promptry CLI.

Each module in this package defines the commands for one Typer sub-app
(prompt, monitor, templates, dataset, garak, new). They register against
the shared app/sub-apps defined in :mod:`promptry.commands._base`.
"""
